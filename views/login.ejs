<!DOCTYPE html>
<html lang="en">
    <% include partials/_header %>

    <body style="font-size:20px; padding:50px;">
        <div class="container p-3 mb-2 bg-light text-dark">
            <form method="POST" action="/login" style="margin:150px;">
                <fieldset>
                    <legend>Sign-in and start shortening!</legend>
                        <label for ="email">Enter email:</label>
                            <input id="email" name="email" type="text" placeholder="" style="width: 200px">
                        <label for ="password">Password:</label>
                            <input id="password" name="password" type="password" placeholder="" style="width: 200px">
                            <button class="btn btn-secondary" type="submit">Login</button>
                </fieldset>
            </form>
            
                <% if (error) { %>
                    <div class="alert alert-danger" role="alert" style="text-align: center">
                    <%= error %>
                    </div>
                <% } %>
           
        </div>
    </body>
</html>
